import type { NextConfig } from "next";

/**
 * Next.js uygulama yapılandırması.
 * Bu dosya, sunucu ve istemci tarafı paketlemesi, yönlendirme kuralları,
 * ortam değişkenleri ve diğer derleme zamanı yapılandırmalarını tanımlar.
 * @see https://nextjs.org/docs/api-reference/next.config.js/introduction
 */
const nextConfig: NextConfig = {
  /* Yapılandırma seçenekleri buraya yazılır */
};

export default nextConfig;
